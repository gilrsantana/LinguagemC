#include <stdio.h>
#include <time.h>


int main (void)
{
/*  time_t inicio, fim;
  double razao = 0.0;
  char nome[50];

  time (&inicio);
  printf ("Digite seu nome:\n");
  gets (nome);
  time (&fim);
  razao = difftime (fim, inicio);
  printf ("%s, voce demorou %.2lf segundos para digitar seu nome\n", nome, razao); //Arredondamento de 2 casas decimais
*/
  return 0;
}
